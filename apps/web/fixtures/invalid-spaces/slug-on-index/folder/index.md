---
title: Folder
slug: nope
---

A folder index.md with `slug:` set is semantically incoherent — the URL
segment for an index.md comes from the parent directory, not the filename,
so there's nothing for the slug to replace.

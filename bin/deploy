#!/usr/bin/env bash
set -euo pipefail

SERVER="${AMBER_DEPLOY_HOST:-amber@178.105.56.249}"
REMOTE_DIR="${AMBER_DEPLOY_PATH:-/home/amber/Amber}"

ssh -o BatchMode=yes "$SERVER" "cd '$REMOTE_DIR' && git pull --ff-only && docker compose up -d"
